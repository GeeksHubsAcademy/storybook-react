const message = `
Importa los componentes individualmente,  por ejemplo:

import Button from "storybook-react-geekshubs/dist/components/Buttons/Button";
`;

console.warn(message);

export default message;
